using System;

namespace EcsLte.Exceptions
{
    /*public class SharedKeyWrongTypeException : EcsLteException
    {
        public SharedKeyWrongTypeException(Type correctType, Type invalidType)
            : base($"SharedKey<{nameof(correctType)}> tried to use the wrong component type of '{nameof(invalidType)}'.",
                "Use the same type when using shared key.")
        {
        }
    }*/
}